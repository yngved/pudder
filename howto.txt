git tutorial.
1. written in branch 'home-branch'
2. Continue to test on branch 'home-branch'
3. We are now working in the 'git-tutorial' branch
3.1 I do now work in the 'git-tutorial' branch

#624shsilkfbk da Push branch to github
git push origin 'branch' (git push origin branch)

#2 Use git fetch/git merge instead of git pull
- git pull does a lot of magic
- try to merge this into origin master 
